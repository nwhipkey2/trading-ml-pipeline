[project]
name = "stock-ml-pipeline"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "pandas>=2.0",
  "numpy>=1.24",
  "pyarrow>=12.0",
  "pydantic>=2.0",
  "scikit-learn>=1.3",
  "python-dotenv>=1.0"
]

[tool.black]
line-length = 100
